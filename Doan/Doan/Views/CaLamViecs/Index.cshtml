@model IEnumerable<Doan.Models.CaLamViec>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/Layout_Menu.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            <h5>Ca làm việc</h5>
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GioBD)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GioKT)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CaLamViec1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GioBD)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GioKT)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaCLV }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaCLV }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaCLV })
        </td>
    </tr>
}

</table>
